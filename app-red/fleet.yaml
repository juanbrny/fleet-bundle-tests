# Default namespace
#namespace: default

targetCustomizations:
- name: prod
  # Custom namespace value overriding the value at the root
  namespace: production
  clusterSelector:
    matchLabels:
      numbertype: odd
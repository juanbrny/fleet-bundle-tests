# Default namespace
#namespace: default

targetCustomizations:
- name: notmanaged
  clusterGroup: singlenodemanaged
 # clusterGroupSelector:
 #    matchExpressions:
 #    - {key: metadata.name, operator: NotIn, values: [singlenodemanaged]}


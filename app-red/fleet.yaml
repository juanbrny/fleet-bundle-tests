# Default namespace
#namespace: default

targetCustomizations:
- name: notmanaged
#  # Custom namespace value overriding the value at the root
  clusterGroupSelector:
     matchExpressions:
     - {key: metadata.name, operator: NotIn, values: [singlenodemanaged]}
#    matchLabels:
#      numbertype: zero

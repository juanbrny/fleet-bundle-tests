# Default namespace
#namespace: default

targetCustomizations:
- name: notmanaged
  clusterGroupSelector:
     matchExpressions:
     - {key: metadata.name, operator: NotIn, values: [singlenodemanaged]}
#    matchLabels:
#      numbertype: zero
